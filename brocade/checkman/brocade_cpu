title: Brocade FibreChannel Switches: CPU utilization
agents: snmp
catalog: hw/storagehw/brocade
license: GPL
distribution: check_mk
description:
 This check monitors the CPU utilization of Brocade FC switches

 Default levels are 80, 90 percent. These are configurable.

inventory:
 The inventory creates one service for each CPU.
